"""Tier 2 simulation backends (§3): fast contact simulation, the coarse filter
before anything expensive. Nothing in this package decides whether a design
passes — it produces measurements, and `engine.criteria` turns them into
verdicts.
"""
