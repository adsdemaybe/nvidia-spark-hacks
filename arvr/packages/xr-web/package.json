{
  "name": "@struct/xr-web",
  "version": "0.1.0",
  "private": true,
  "description": "STRUCT AR/XR browser client. A tracked controller is just another SpatialAdapter (STRUCT_2.md 28).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "loop": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/three": "^0.171.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
